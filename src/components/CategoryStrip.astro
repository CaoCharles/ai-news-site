---
const categories = [
  { name: "Google", color: "bg-blue-100 text-blue-800" },
  { name: "ChatGPT", color: "bg-green-100 text-green-800" },
  { name: "Claude", color: "bg-orange-100 text-orange-800" },
  { name: "Grok", color: "bg-gray-100 text-gray-800" },
];
---

<div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">
  {categories.map((cat) => (
    <a href={`${import.meta.env.BASE_URL.replace(/\/$/, '')}/${cat.name.toLowerCase() === '千問' ? 'qianwen' : cat.name.toLowerCase()}`} class={`block p-3 rounded-sm ${cat.color} flex items-center justify-between cursor-pointer hover:opacity-80 transition-opacity`}>
      <span class="font-bold text-sm">{cat.name}</span>
      <span class="text-xs opacity-60">Trending &rarr;</span>
    </a>
  ))}
</div>
