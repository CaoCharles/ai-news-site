---
const categories = [
  { name: "World News", color: "bg-blue-100 text-blue-800" },
  { name: "Technology", color: "bg-purple-100 text-purple-800" },
  { name: "Health", color: "bg-green-100 text-green-800" },
  { name: "Sports", color: "bg-orange-100 text-orange-800" },
];
---

<div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">
  {categories.map((cat) => (
    <div class={`p-3 rounded-sm ${cat.color} flex items-center justify-between cursor-pointer hover:opacity-80`}>
      <span class="font-bold text-sm">{cat.name}</span>
      <span class="text-xs opacity-60">Trending &rarr;</span>
    </div>
  ))}
</div>
